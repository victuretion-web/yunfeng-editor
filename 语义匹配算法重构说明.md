# 语义匹配算法重构说明

## 本次目标

- 将中插素材选择从“关键词/轮询优先”调整为“语义相似度优先”
- 对同一主视频实现中插素材零重复
- 增加 24 小时跨视频去重记录
- 引入级联补充链路和中插占比监控

## 已落地能力

### 1. 语义优先匹配

- 新增 `semantic_matcher.py`
- 为主视频候选片段和中插素材分别构建语义画像
- 语义画像来源：
  - 字幕文本
  - 文件名
  - 标签
  - 素材所属目录语义
  - 情绪强度
- 真实素材选择时先进行语义排序，再应用阈值过滤
- 主匹配阈值：`>= 0.75`
- 级联复用阈值：`>= 0.60`

### 2. 去重控制

- 同一主视频内：
  - 基于 `content_hash` 建立已用指纹集合
  - 相同中插素材在同一主视频内不再复用
- 跨视频：
  - 新增 `broll_usage_history.json`
  - 默认记录 24 小时内使用过的中插素材
  - 默认阻止 24 小时内重复投放相同 `content_hash`

### 3. 级联补充

- 第一层：当前语义池高阈值匹配
- 第二层：上一支视频已使用素材池低阈值复用
- 第三层：紧急通用素材池
- 紧急通用素材池规模：
  - 默认按全部中插素材的 10% 自动划入

### 4. 占比监控

- 新增中插占比监控报告：
  - 总体目标占比：`>= 65%`
  - 窗口监控：每 15 分钟窗口单独统计
- 输出文件：
  - `*_broll_ratio_report.json`

## 关键文件

- `semantic_matcher.py`
  - 语义画像
  - 相似度计算
  - 历史去重记录
  - 占比分析
- `otc_promo_workflow.py`
  - 接入语义优先选材
  - 接入同视频零重复
  - 接入 24 小时跨视频冷却
  - 输出语义分数和补充层级

## 运行期产物

- `broll_usage_history.json`
  - 记录最近 24 小时中插使用痕迹
- `*_insert_density_config.json`
  - 记录最终落地片段
  - 追加语义分数与补充层级
- `*_broll_ratio_report.json`
  - 记录总体占比与 15 分钟窗口占比
- `*_density_warning.txt`
  - 记录密度未完全达标告警

## 当前限制

- “语义匹配准确率 >= 92%” 需要人工标注集和抽检流程，当前代码仅提供评分框架与报告出口，尚未形成可审计的标注闭环
- “去重成功率 100%” 在当前实现中针对 `content_hash` 是强约束，但前提是素材文件哈希正确生成且历史文件未被外部清空
- “占比合规率 >= 98%” 需要批量样本回放统计，当前已具备监控与告警能力，尚未形成离线批量验收报表
- “系统响应时间 <= 200ms” 需要独立压测环境；当前实现优先保证语义质量和去重规则，未对全链路性能做最终优化承诺

## 后续建议

- 增加离线标注集，建立语义匹配人工抽检脚本
- 增加批量回放脚本，自动汇总：
  - 语义命中率
  - 去重命中率
  - 占比达标率
- 视性能结果决定是否引入：
  - 缓存化语义画像
  - 预构建素材索引
  - 更高阶向量模型
