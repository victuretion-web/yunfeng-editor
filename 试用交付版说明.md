# 云锋剪辑试用交付版说明

## 1. 本次试用目标

- 面向小范围同事灰度试用，优先验证“能否稳定生成草稿”和“界面是否容易理解”。
- 本轮不要求同事直接修改内部文件，也不要求他们理解项目代码。
- 如遇异常，优先保留 `output/` 目录，便于回收日志排查。

## 2. 发给同事的内容

- 整个 `dist/YunFengEditor/` 目录
- `发布使用说明.md`
- 本文件 `试用交付版说明.md`
- `试用发包清单.md`
- `试用通知文案.md`
- `试用反馈模板.md`

不要只发 `YunFengEditor.exe` 单文件。
正式转发前，先按 `试用发包清单.md` 自查一次。

## 3. 同事首次使用步骤

1. 解压整个发布目录到本地磁盘，不要在压缩包内直接运行。
2. 双击 `YunFengEditor.exe` 启动。
3. 先填写素材目录和 `API Key`。
4. 保持默认参数先跑 1 条口播测试，不要一上来整批全跑。
5. 到任务看板确认:
   - 状态是否成功
   - 是否生成审查报告
   - 是否能打开“草稿复查”

## 4. 关于“没看到草稿”

- 如果任务成功，但剪映首页里没看到草稿，不一定是生成失败。
- 当前版本在系统剪映草稿目录不可写时，会自动把草稿保存在便携目录。
- 这时界面会明确提示“本次草稿不会直接出现在剪映首页”。
- 同事应在任务看板点击“打开草稿复查”，而不是直接判定失败。

## 5. 建议你收集的反馈

- 哪台机器上首次启动失败
- 哪台机器上联通测试失败
- 哪些任务成功但草稿落在便携目录
- 哪些素材目录结构最容易让同事填错
- 哪些提示语仍然不够直观

建议直接要求同事按 `试用反馈模板.md` 回传，减少来回追问。

## 6. 同事反馈时请一起打包这些文件

- `output/task_logs/` 下对应任务日志
- `output/internal_maintenance.log`
- `output/ui_task_report.json`
- 如有草稿但不在剪映首页，附上任务看板里的草稿目录路径

## 7. 当前试用版结论

- 已通过发布验包
- 已通过 GUI 启动烟测
- 已通过最小样本草稿生成烟测
- 已加入“便携草稿目录”明确提示，减少“误以为没生成”的情况
