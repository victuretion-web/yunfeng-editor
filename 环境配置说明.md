# 云锋剪辑环境配置说明

## 1. 项目形态

- 当前项目是 `Python + tkinter + PyInstaller` 的 Windows 桌面应用。
- 不是 Node/Web 前端项目，不需要 `node_modules`、`npm build` 或静态站点部署。
- 发布产物为 Windows 绿色版目录与压缩包。

## 2. 运行环境要求

- 操作系统：Windows 10 / 11 64 位
- 剪映版本：专业版 `5.9.x`
- 运行发布包：不需要额外安装 Python
- 本地开发 / 重新打包：建议使用 `Python 3.13 x64`
- 推荐内存：`16 GB` 或更高
- 建议可用磁盘空间：至少 `6 GB`

## 3. 第三方依赖说明

- 核心运行依赖来自 `requirements.txt`
- 打包依赖来自 `requirements-dev.txt`
- 媒体能力：
  - `ffmpeg` / `ffprobe`
  - `openai-whisper`
  - `torch`
  - `opencv-python`
  - `pydub`
  - `pymediainfo`
- 剪映能力：
  - `jianying-editor-skill-main/jianying-editor-skill-main`
- Python 3.13 兼容补充：
  - `audioop-lts==0.2.2`
  - 根目录 `pyaudioop.py` 作为 `pydub` 兼容桥接模块

## 4. 本地开发安装步骤

### 4.1 创建开发环境

```powershell
py -3.13 -m venv .venv_release
.\.venv_release\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
```

### 4.2 启动源码版本

```powershell
.\.venv_release\Scripts\python.exe ui_main.py
```

### 4.3 Worker 自检

```powershell
.\.venv_release\Scripts\python.exe app_launcher.py --worker --help
.\.venv_release\Scripts\python.exe app_launcher.py --worker --preflight
```

## 5. 生产打包步骤

### 5.1 打包前准备

- 确保目录存在：`ffmpeg-8.1-essentials_build/`
- 确保目录存在：`jianying-editor-skill-main/jianying-editor-skill-main/`
- 构建脚本会自动准备 Whisper `base` 模型到 `.release_assets/whisper/base.pt`

### 5.2 执行打包

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1
```

### 5.3 默认输出

- 发布目录：`dist/YunFengEditor/`
- 压缩包：`dist/YunFengEditor-portable-win64.zip`
- 校验报告：`dist/YunFengEditor/release_verification.json`

## 6. 发布包启动方式

### 6.1 同事使用发布包

1. 解压 `YunFengEditor-portable-win64.zip`
2. 保持 `YunFengEditor.exe` 与 `_internal/` 同级
3. 双击 `YunFengEditor.exe`

### 6.2 发布包命令行验证

```powershell
.\YunFengEditor.exe --worker --help
.\YunFengEditor.exe --worker --preflight
```

## 7. 配置来源说明

- 本项目没有 `.env` / `.env.production` 文件
- 配置来源主要有三类：
  - 运行时环境变量
  - GUI 本地配置文件 `output/ui_settings.json`
  - 打包时内置资源目录 `_internal/`

### 7.1 常用环境变量

- `OTC_APP_RESOURCE_DIR`
- `OTC_APP_RUNTIME_DIR`
- `OTC_OUTPUT_DIR`
- `OTC_WHISPER_CACHE_DIR`
- `OTC_RELEASE_SKILL_ROOT`
- `OTC_RELEASE_FFMPEG_ROOT`
- `OTC_RELEASE_WHISPER_MODEL`
- `LLM_API_KEY`
- `LLM_BASE_URL`
- `LLM_MODEL`

### 7.2 大模型接口说明

- GUI 锁定的基础地址为：`https://api.kuai.host/v1`
- 若未配置 `LLM_API_KEY`，程序可运行，但大模型相关能力不会通过鉴权
- 本次验证中，服务地址可达：
  - `GET /v1/models` 返回 `401`
  - 说明网络连通正常，但当前环境无有效鉴权信息

## 8. 自动化验证内容

本次发布流程已验证：

- 干净 `.venv_release` 环境全量安装依赖
- `pip check` 无冲突
- 关键模块导入成功：
  - `torch`
  - `whisper`
  - `opencv-python`
  - `openai`
  - `uiautomation`
- `app_launcher.py --worker --help` 通过
- `app_launcher.py --worker --preflight` 通过
- `jianying-editor` skill 诊断通过
- `build_release.ps1` 打包通过
- `verify_release.py` 校验通过
- 产物内资源存在性检查通过
- 打包后 worker 冒烟、自检、GUI 存活、样本草稿生成全部通过

## 9. 页面/界面渲染说明

- 当前项目为桌面 GUI，不存在 Web 页面静态资源渲染链路
- 已通过以下方式降低闪屏/黑框风险：
  - `PyInstaller` 以 `console=False` 打包
  - 子进程统一走 `subprocess_windows.py`
  - 发布验收包含 GUI 启动存活测试
- 当前自动化环境无法像人工一样直接目视判断“绝对无频闪”
- 但代码路径、打包参数和自动化烟测结果均未发现异常

## 10. 交付建议

- 对同事交付时，优先发送：
  - `dist/YunFengEditor-portable-win64.zip`
- 不要只发送单个 `YunFengEditor.exe`
- 不要拆散 `_internal/` 目录
- 同事若只需使用，不需要安装 Python
- 同事若需要重新打包，请严格按本说明从源码构建
